# 결정 기록

## 2026-09-10 설계

**월 1회가 아니라 반달 2회로 간다.**
페이스북 예약은 지금부터 29일 이내만 허용한다. 1일에 30건을 몰면 말일 건이 창을 벗어난다.
반달로 쪼개면 검수 부담도 15건씩 나뉘고 트렌드 신선도도 덜 상한다.

**매일 스케줄러 대신 예약 위임을 골랐다.**
제작도 검수도 월 2회인데 발행만 매일 PC를 켤 이유가 없다. 실패 지점이 30개에서 2개로 준다.
대가로 예약 후 내용 수정을 포기했다. 메타 비즈니스 스위트에서 수동으로 한다.

**원고 생성에 LLM API를 쓰지 않는다.**
`plan.py`는 주제 선택과 날짜 배치까지만 한다. 원고는 Claude Code 세션에서 채운다.
어차피 사람 검수가 그 자리에 들어가므로 단계가 하나 준다. API 키도 불필요하다.

**중복 방지를 최우선 요구사항으로 뒀다.**
예약이 중복 등록되면 같은 글이 두 번 올라간다. 페이지 신뢰도에 직접 타격이다.
`state.json` 건별 커밋으로 막는다. 전체 롤백은 불가능하므로 시도하지 않는다.

**템플릿 3종 로테이션은 디자인 취향이 아니라 스팸 판정 회피다.**
같은 시각 패턴이 반복되면 도달이 눌린다.

**인스타 자동화와 코드를 공유하지 않는다.**
계정도 콘텐츠도 독립이다. Playwright 렌더 + ffmpeg 합성 패턴만 참고했다.
페이스북은 로컬 파일 직접 업로드가 되므로 인스타에 필요했던 Cloudinary 호스팅 단계가 통째로 빠졌다.

## 2026-09-13 완료

구현 완료, 실토큰 스모크 테스트는 보류.
